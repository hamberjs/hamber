<script>
	import Widget from './Widget.hamber';
	import Inner from './Inner.hamber';

	export let logs = [];
	export { Inner };

	function foo() {
		logs.push(this);
	}
	function bar() {
		logs.push(this);
	}
</script>

<Widget on:click={foo} on:bar={bar} />
