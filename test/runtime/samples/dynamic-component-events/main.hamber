<script>
	import Foo from './Foo.hamber';
	import Bar from './Bar.hamber';

	export let x;
	export let selected;
</script>

<hamber:component this="{x ? Foo : Bar}" on:select="{e => selected = e.detail.id}"/>
