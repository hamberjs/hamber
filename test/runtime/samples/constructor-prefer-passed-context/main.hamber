<script>
	import { setContext } from 'hamber';
	import ChildComponent from './ChildComponent.hamber';

	setContext('foo', true);

	function render(node) {
		new ChildComponent({
			target: node,
			context: new Map()
		});
	}
</script>

<div use:render />
