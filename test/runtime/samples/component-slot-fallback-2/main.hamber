<script>
	import Outer from "./Outer.hamber";
	import Inner from "./Inner.hamber";
	import {model} from "./store.hamber";

	export let props = '';
	export let fallback = '';

	export function getSubscriberCount() {
		return model.getCount();
	}
</script>

<Outer slotProps={props} defaultValue={fallback} let:slotProps>
	<Inner value={slotProps} />
</Outer>

<Outer slotProps={props} defaultValue={fallback} let:slotProps>
	<Inner value={slotProps} />
</Outer>

<Outer slotProps={props} defaultValue={fallback}>
</Outer>
