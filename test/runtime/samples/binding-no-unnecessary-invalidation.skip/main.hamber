<script>
	import { writable } from "hamber/store";
	import Tab from "./Tab.hamber";

	let i = 0;
	const { set, subscribe } = writable({ id: 1, name: "tab1" });
	const tab = {
		set(value) {
			i++;
			set(value);
		},
		subscribe,
	};
</script>

<Tab bind:tab={$tab} />
<p>{i}</p>
