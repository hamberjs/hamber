<script>
	import { beforeUpdate } from 'hamber';

	import Widget from './Widget.hamber';

	export let foo = { x: 1 };
	export let bar = { x: 1 };

	beforeUpdate(() => {
		bar = foo;
	});
</script>

<Widget foo='{foo}' bar='{bar}'/>
